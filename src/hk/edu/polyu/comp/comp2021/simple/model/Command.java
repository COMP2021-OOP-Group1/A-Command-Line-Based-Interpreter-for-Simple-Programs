package hk.edu.polyu.comp.comp2021.simple.model;

public enum Command {
    VARDEF(), BINEXPR(), UNEXPR(),ASSIGN(), PRINT(), BLOCK(), IF(), WHILE(), PROGRAM(), EXECUTE(), LIST(), STORE(), LOAD(), QUIT();
}
